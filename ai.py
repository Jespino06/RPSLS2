from player import Player
import random

class Ai(Player):
    def __init__(self, name):
        super().__init__(name)


def spur_list(list):
    self.chosen_gesture = (random.choice(self.gesture_list))
   
